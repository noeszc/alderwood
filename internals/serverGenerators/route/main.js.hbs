import express from 'express';
import {{properCase name}} from 'server/routes/{{properCase name}}/{{properCase name}}.route';

const router = express.Router();

router.use('/', {{properCase name}});

export default router;
